apiVersion: v1
kind: Service
metadata:
  name: knowledge-base-api
  labels:
    app: knowledge-base-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: knowledge-base-api